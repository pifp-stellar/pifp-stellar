[package]
name = "indexer"
version = "0.1.0"
edition = "2021"
description = "PIFP event indexer — streams Soroban on-chain events and exposes a REST API"
license = "MIT"

[[bin]]
name = "indexer"
path = "src/main.rs"

[dependencies]
actix = "0.13"
axum = "0.8"
tokio = { version = "1", features = ["full"] }
sqlx = { version = "0.8", features = ["runtime-tokio-rustls", "sqlite", "postgres", "macros", "migrate"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
reqwest = { version = "0.12", features = ["json", "rustls-tls"], default-features = false }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
anyhow = "1"
thiserror = "2"
dotenvy = "0.15"
tower-http = { version = "0.6", features = ["cors", "trace"] }
chrono = { version = "0.4", features = ["serde"] }
base64 = "0.22"
hex = "0.4"
tokio-util = "0.7"
ed25519-dalek = { version = "2", features = ["serde", "rand_core"] }
stellar-strkey = "0.0.16"
redis = { version = "1.2", features = ["tokio-comp"] }
hmac = "0.13"
sha2 = "0.11"
async-trait = "0.1"
prometheus = { version = "0.14", features = ["process"] }
lazy_static = "1"
sysinfo = "0.38"
tract-onnx = "0.22"
stellar-xdr = { version = "25.0.0", features = ["std"] }
governor = { version = "0.10", features = ["dashmap"] }
dashmap = "6"
# GraphQL
async-graphql = { version = "7.0", features = ["chrono", "dataloader"] }
async-graphql-axum = "7.0"

# Lock-free B+ tree engine (Issue #4)
crossbeam-epoch = "0.9"
memmap2 = "0.9"

# Sentry error tracking
sentry = { version = "0.47", default-features = false, features = ["backtrace", "contexts", "panic", "reqwest", "rustls"] }
sentry-tracing = "0.47"
tower = "0.5"
futures-util = "0.3"
tokio-tungstenite = "0.26.2"

[dev-dependencies]
tokio = { version = "1", features = ["full"] }
rand = "0.10"
axum-test = "20.0.0"
